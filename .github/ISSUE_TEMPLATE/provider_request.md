---
name: Provider request
about: Ask for built-in support for an AI service
labels: provider
---

**Service**

**Usage/quota endpoint** (URL and auth header it expects)

**Example response** (redact the numbers if you like, keep the shape)

```json
```

Note: if the endpoint returns JSON, you may not need a code change at all —
see [custom providers](../../README.md#custom-providers), which map a response
to a gauge through JSON pointers in `credentials.json`.
